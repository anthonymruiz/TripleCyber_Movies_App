import { ToShortOverviewPipe } from './to-short-overview.pipe';

describe('ToShortOverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new ToShortOverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
